package com.kitchensaver.backend.model;

//enum that defines the different user roles

public enum Role {
    // Admin role with full access
    ADMIN,

    // Cabinet Maker Installer role with specific access
    CABINET_MAKER,
    INSTALLER
}
